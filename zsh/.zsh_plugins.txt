# Antidote plugin bundle
# One plugin per line: <github-user>/<repo> [options]
# Run `antidote bundle < .zsh_plugins.txt > .zsh_plugins.zsh` to rebuild,
# or just edit this file — .zshrc regenerates it automatically.

# Useful OMZ plugins (antidote can pull individual paths from oh-my-zsh)
ohmyzsh/ohmyzsh path:plugins/git
ohmyzsh/ohmyzsh path:plugins/sudo
ohmyzsh/ohmyzsh path:plugins/command-not-found
ohmyzsh/ohmyzsh path:plugins/extract

# Completions (load before syntax highlighting / autosuggestions)
zsh-users/zsh-completions

# Autosuggestions - suggests commands as you type based on history
zsh-users/zsh-autosuggestions

# Syntax highlighting - MUST be loaded last
zsh-users/zsh-syntax-highlighting